export interface Project {
  name: string;
  description: string;
  techStack: string[];
  features: string[];
  demoUrl: string;
  repoUrl: string;
  image: string;
}

export interface SkillGroup {
  category: string;
  skills: string[];
}
